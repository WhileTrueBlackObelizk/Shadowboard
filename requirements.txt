# --- Verschlüsselung ---
cryptography>=41.0.0

# --- Terminalfarben & Styling ---
colorama>=0.4.6

# --- Optionale Extras für ASCII-Banner / Terminal-Handling ---
pyfiglet>=0.9.0
blessed>=1.20.0

# --- Logging & Utilities (falls noch nicht Standardbibliothek genutzt wird) ---
# (normalerweise keine extra Pakete nötig)
